# 2026-09-08

## day16 异常处理(复习/批注答疑)
- 讲解了 `01-cpp/practice/day16/Resource.cpp` 的两条批注(按用户要求只教、不改文件):
  1. 禁止复制:编译器默认生成的复制构造/复制赋值不会 `++m_count`,析构却照常 `--`,计数漂移。修复 = `Resource(const Resource&) = delete;` + `operator= = delete`,把误用挡在编译期。
  2. 加 `static int count() noexcept { return m_count; }` 只读访问器,在两次 `process()` 后输出/断言为 0,验证异常路径的栈展开仍正确析构。
- 将 `04-interview/day16-异常处理/day16.md` 第 2–5 题答案落盘(用户先自写了一版 → 选择"合并补全",保留其框架、补全裸指针泄漏/terminate/容器批量析构/copy-and-swap/无保证档等要点)。
- 惯例:interview 理论题答案直接写 md;代码练习批注以讲解为主,先问是否落盘;用户偏好保留自己作答 + 合并补全。
