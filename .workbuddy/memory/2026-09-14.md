# 2026-09-14 工作记录

## VSCode Python 联想函数补齐（cpp-python-review）

### 改动
- 新建 `.vscode/extensions.json`，推荐 `ms-python.python` + `ms-python.vscode-pylance`，跨项目打开自动提示安装。
- 修改 `.vscode/settings.json`：原 C/C++ Runner 段一字未动，末尾追加：
  - `python.languageServer = "Pylance"`
  - `python.analysis.autoImportCompletions / completeFunctionParens / typeCheckingMode=basic`
  - `editor.quickSuggestions.strings + other = true`（核心：键入即触发）
  - `editor.suggest.snippetsPreventQuickSuggestions = false`
  - `files.associations: {"*.py": "python"}`
- JSON 语法校验：两个文件均通过 `json.loads()`。

### 根因
工程之前只有 C/C++ 配置，Python 这边无语言服务器 + 无扩展推荐，导致 `02-python/day1/clean_and_batch_paths.py` 编辑时无法联想。

### 跨项目复用建议
D:\Learning Documents\WorkFinding\ 下其它 C/C++ 工程若要引入 Python，直接把这两个文件复制过去即可（C++ 段与具体工具链相关，需按本地实际调整）。
